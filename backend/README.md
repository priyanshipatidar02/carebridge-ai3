# CareBridge AI Backend

## Setup
```bash
cd backend
npm install
cp .env.example .env
npm run dev
```

API runs on `http://localhost:5000` by default.

Never put API keys in the frontend. Gemini and Maps calls run only in this backend.
